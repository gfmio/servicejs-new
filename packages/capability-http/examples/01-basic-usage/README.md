# HTTP Mocking Example

Demonstrates HTTP mocking for testing with mock responses and request capture.

## Features

- Mock HTTP responses with status, headers, body
- Pattern matching (string, regex, function)
- Request capture for verification
- JSON response handling

## Usage

```bash
bun start
```
